# ============================================================
# 数据问答 Agent（services/agent）依赖
# ============================================================
#
# 只装四样东西：
#   fastapi / uvicorn       对外接口（与数据服务同一套，便于统一部署）
#   openai                  DeepSeek 提供 OpenAI 兼容接口，官方 Tool Calls
#                           示例就是用这个 SDK；不自己拼 HTTP，
#                           少一类边界情况的出错机会
#   httpx                   openai SDK 的传输层依赖（显式声明，避免隐式拉取）
#
# 版本用区间约束而不是 latest：允许小版本浮动，但必须可复现。
#
# 为什么**不**引入 langchain / langgraph：
#   Sprint 7 的目标是"自然语言转 SQL 的基础能力"，
#   一个带工具调用的循环就够了（见 app/agent.py 的模块说明）。
#   LangGraph 属于 Sprint 8，提前引入会让"这一轮调了什么工具"
#   被图的状态机掩盖，反而不利于本 Sprint 最需要的可验证性。
# ============================================================

fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
openai>=1.50,<3.0
httpx>=0.27,<1.0
