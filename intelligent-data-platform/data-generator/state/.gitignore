# ============================================================
# Data Generator 运行时状态
# ============================================================
#
# 数据生成器写入 MySQL 后，会把本次生成结果快照保存到
# dataset_snapshot.json，使 Kafka 事件生成器能够基于
# 「真实写入 MySQL 的订单/支付数据」产生事件，
# 从而保证 MySQL 与 Kafka 之间的业务一致性。
#
# 这些属于运行时产物，不入 Git。
# ============================================================

# 本次生成结果的快照
dataset_snapshot.json

# 生成过程中可能产生的中间文件
*.tmp
*.log
